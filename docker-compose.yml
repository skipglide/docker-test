version: '3'

services:
  dtest:
    build:
      context: .
      dockerfile: ./docker/django/Dockerfile
    image: skipglide/dtest:latest
    volumes:
      - .:/code
      - ./static:/usr/share/nginx/html/static
    ports:
      - 8000:8000
  nginx:
    image: skipglide/nginx:latest
    build:
      context: .
      dockerfile: ./docker/nginx/Dockerfile
    ports:
      - 80:80
    volumes:
      - ./nginx:/etc/nginx
      - ./static:/usr/share/nginx/html/static